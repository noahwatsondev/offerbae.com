<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <meta name="description" content="<%= description %>">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --primary: #4F46E5;
            --primary-light: #EEF2FF;
            --text-main: #111827;
            --text-secondary: #4B5563;
            --bg-body: #F9FAFB;
            --card-bg: #FFFFFF;
            --border: #E5E7EB;
            --danger: #EF4444;
            --success: #10B981;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg-body);
            color: var(--text-main);
            line-height: 1.5;
        }

        /* Navigation */
        nav {
            background: #fff;
            padding: 1rem 2rem;
            border-bottom: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--primary);
            text-decoration: none;
            letter-spacing: -1px;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
            display: grid;
            grid-template-columns: 280px 1fr;
            gap: 2rem;
        }

        @media (max-width: 1024px) {
            .container {
                grid-template-columns: 1fr;
                padding: 1rem;
            }

            aside {
                display: none;
            }

            /* Hide filters on mobile by default or use drawer */
        }

        /* Sidebar Filters */
        aside {
            background: #fff;
            border-radius: 1rem;
            padding: 1.5rem;
            border: 1px solid var(--border);
            height: fit-content;
        }

        .filter-group {
            margin-bottom: 2rem;
        }

        .filter-title {
            font-weight: 700;
            font-size: 1rem;
            margin-bottom: 1rem;
            display: block;
            border-left: 4px solid var(--primary);
            padding-left: 0.75rem;
        }

        .filter-option {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
            cursor: pointer;
        }

        .filter-option:hover {
            color: var(--primary);
        }

        /* Main Content */
        .catalog-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 2rem;
        }

        .catalog-title {
            font-size: 2rem;
            font-weight: 800;
            letter-spacing: -1px;
        }

        .catalog-meta {
            color: var(--text-secondary);
            font-size: 0.875rem;
        }

        .controls {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        select {
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            border: 1px solid var(--border);
            font-family: inherit;
            font-size: 0.875rem;
        }

        /* Product Grid */
        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .product-card {
            background: var(--card-bg);
            border-radius: 1rem;
            overflow: hidden;
            border: 1px solid var(--border);
            transition: transform 0.3s, box-shadow 0.3s;
            display: flex;
            flex-direction: column;
            text-decoration: none;
            color: inherit;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.05);
            border-color: var(--primary);
        }

        .product-image-wrapper {
            position: relative;
            padding-bottom: 100%;
            overflow: hidden;
            background: #fff;
        }

        .product-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: contain;
            padding: 1rem;
            transition: transform 0.5s;
        }

        .product-card:hover .product-image {
            transform: scale(1.05);
        }

        .sale-badge {
            position: absolute;
            top: 1rem;
            left: 1rem;
            background: var(--danger);
            color: #fff;
            padding: 0.25rem 0.75rem;
            border-radius: 2rem;
            font-size: 0.75rem;
            font-weight: 700;
            box-shadow: 0 4px 6px rgba(239, 68, 68, 0.2);
        }

        .product-info {
            padding: 1.5rem;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .brand-label {
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--primary);
            text-transform: uppercase;
            margin-bottom: 0.5rem;
            letter-spacing: 0.5px;
        }

        .product-name {
            font-size: 1.063rem;
            font-weight: 600;
            line-height: 1.3;
            color: var(--text-main);
            margin-bottom: 1rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            height: 2.6em;
        }

        .price-row {
            display: flex;
            align-items: baseline;
            gap: 0.75rem;
            margin-top: auto;
        }

        .current-price {
            font-size: 1.25rem;
            font-weight: 800;
            color: var(--text-main);
        }

        .was-price {
            font-size: 0.875rem;
            color: var(--text-secondary);
            text-decoration: line-through;
        }

        .redeem-btn {
            margin-top: 1.5rem;
            width: 100%;
            padding: 0.75rem;
            border-radius: 0.75rem;
            background: var(--primary-light);
            color: var(--primary);
            border: none;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            transition: all 0.2s;
        }

        .product-card:hover .redeem-btn {
            background: var(--primary);
            color: #fff;
        }

        /* Pagination */
        .pagination {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-top: 4rem;
            padding-bottom: 4rem;
        }

        .page-btn {
            padding: 0.75rem 1.5rem;
            border-radius: 0.75rem;
            border: 1px solid var(--border);
            background: #fff;
            font-weight: 600;
            text-decoration: none;
            color: var(--text-main);
            transition: all 0.2s;
        }

        .page-btn:hover {
            border-color: var(--primary);
            color: var(--primary);
        }

        .page-btn.disabled {
            opacity: 0.5;
            pointer-events: none;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            background: #fff;
            border-radius: 1rem;
            border: 1px solid var(--border);
        }
    </style>
</head>

<body>
    <nav>
        <a href="/" class="logo">OFFERBAE</a>
    </nav>

    <div class="container">
        <aside>
            <div class="filter-group">
                <span class="filter-title">Price Range</span>
                <label class="filter-option"><input type="checkbox"> Under $25</label>
                <label class="filter-option"><input type="checkbox"> $25 - $50</label>
                <label class="filter-option"><input type="checkbox"> $50 - $100</label>
                <label class="filter-option"><input type="checkbox"> $100+</label>
            </div>

            <% if (type==='category' ) { %>
                <div class="filter-group">
                    <span class="filter-title">Brands</span>
                    <!-- We could list brands here -->
                </div>
                <% } %>

                    <div class="filter-group">
                        <span class="filter-title">Availability</span>
                        <label class="filter-option"><input type="checkbox"> On Sale Only</label>
                        <label class="filter-option"><input type="checkbox"> Promo Codes Available</label>
                    </div>
        </aside>

        <main>
            <div class="catalog-header">
                <div>
                    <h1 class="catalog-title">
                        <%= (type==='brand' ? data.name : data.name) %>
                    </h1>
                    <p class="catalog-meta">Showing deals from top merchants</p>
                </div>
                <div class="controls">
                    <select onchange="window.location.search = '?sort=' + this.value">
                        <option value="newest" <%=sort==='newest' ? 'selected' : '' %>>Latest Arrivals</option>
                        <option value="price-low" <%=sort==='price-low' ? 'selected' : '' %>>Price: Low to High</option>
                        <option value="price-high" <%=sort==='price-high' ? 'selected' : '' %>>Price: High to Low
                        </option>
                    </select>
                </div>
            </div>

            <% if (products.length===0) { %>
                <div class="empty-state">
                    <h3>No products found</h3>
                    <p>Try adjusting your filters or check back later.</p>
                </div>
                <% } else { %>
                    <div class="product-grid">
                        <% products.forEach(p=> { %>
                            <% const brandSlug=p.advertiserName ? p.advertiserName.toLowerCase().replace(/\s+/g, '-' )
                                : 'brand' ; const productUrl=`/${brandSlug}/${p.slug}`; %>
                                <a href="<%= productUrl %>" class="product-card">
                                    <div class="product-image-wrapper">
                                        <% if (p.salePrice && p.price> p.salePrice) { %>
                                            <div class="sale-badge">SALE</div>
                                            <% } %>
                                                <img src="<%= p.storageImageUrl || p.imageUrl || '/placeholder.png' %>"
                                                    alt="<%= p.name %>" class="product-image" loading="lazy">
                                    </div>
                                    <div class="product-info">
                                        <span class="brand-label">
                                            <%= p.advertiserName %>
                                        </span>
                                        <h3 class="product-name">
                                            <%= p.name %>
                                        </h3>
                                        <div class="price-row">
                                            <% if (p.salePrice && p.price> p.salePrice) { %>
                                                <span class="current-price">$<%= p.salePrice.toLocaleString() %></span>
                                                <span class="was-price">$<%= p.price.toLocaleString() %></span>
                                                <% } else { %>
                                                    <span class="current-price">$<%= (p.price || 0).toLocaleString() %>
                                                    </span>
                                                    <% } %>
                                        </div>
                                        <button class="redeem-btn">
                                            <svg width="18" height="18" fill="none" viewBox="0 0 24 24"
                                                stroke="currentColor">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                                            </svg>
                                            View Details
                                        </button>
                                    </div>
                                </a>
                                <% }); %>
                    </div>

                    <div class="pagination">
                        <a href="?page=<%= page - 1 %>&sort=<%= sort %>"
                            class="page-btn <%= page === 1 ? 'disabled' : '' %>">Previous</a>
                        <a href="?page=<%= page + 1 %>&sort=<%= sort %>"
                            class="page-btn <%= !hasNextPage ? 'disabled' : '' %>">Next</a>
                    </div>
                    <% } %>
        </main>
    </div>

    <footer
        style="margin-top: 4rem; padding: 4rem 2rem; border-top: 1px solid var(--border); background: #fff; text-align: center;">
        <p style="color: var(--text-secondary); font-size: 0.875rem;">&copy; 2026 Offerbae. Premium Fashion & Lifestyle
            Deals.</p>
    </footer>
</body>

</html>