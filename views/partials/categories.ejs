<div class="categories-wrapper">
    <div class="categories-scroll">
        <% if (typeof categories !=='undefined' && categories.length> 0) { %>
            <% categories.forEach(cat=> { %>
                <a href="/layout/categories/<%= cat.slug %>" class="category-pill">
                    <%= cat.name %>
                </a>
                <% }) %>
                    <% } %>
    </div>
</div>